#include <stdio.h>
int main (){
	int num;
	int div;
	int result;
	printf ("Introduce una un numero entero positivo ");
	scanf ("%d",&num);
	if (num<0) {
		printf ("Numero no valido, tiene que ser positivo y entero ");
	}
	int calculaDivisores (int num) {
		for (div=1; div<=num; div++){
			return div++;
			
			do {
			 result = num/div++;
			}
			while (result == );
			
			if (result != int){
				return 0;
			}
			
			
		}  
	}	
	
	printf("el numero de divisores son = %d ", result);
	return 0;
}
